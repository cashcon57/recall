{
  "_comment": "Example MCP client configuration for Recall. Works with Claude Code (.mcp.json), Claude Desktop (claude_desktop_config.json), Cursor, Windsurf, and any MCP-compatible client.",
  "mcpServers": {
    "recall": {
      "type": "http",
      "url": "https://YOUR-WORKER-SUBDOMAIN.workers.dev/mcp",
      "headers": {
        "Authorization": "Bearer ${RECALL_API_KEY}"
      }
    }
  }
}
